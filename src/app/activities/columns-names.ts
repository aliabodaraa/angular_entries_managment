import { TableColumns } from '../models/table-inputs';

export const columns: TableColumns = {
  keys: [
    'dc:title',
    'dc:creator',
    'dc:description',
    'dc:created',
    'dc:modified',
  ],
  values: ['title', 'creator', 'description', 'created', 'modified'],
};
